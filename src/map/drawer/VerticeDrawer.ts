import { GraphicalTerritory } from "../GraphicalTerritory";

export interface VerticeDrawerObject {
  vertices: GraphicalTerritory[];
  xOffset: number;
  yOffset: number;
  ratio: number;
  verticeSize: number;
}